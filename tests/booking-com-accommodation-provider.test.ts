import test from 'node:test'; import assert from 'node:assert/strict';
import { BookingComAccommodationProvider } from '../src/infrastructure/providers/booking/booking-com-accommodation-provider.js';
const destination={id:'city:FCO',airportCode:'FCO',city:'Rome',countryCode:'IT',bookingCityId:-126693}; const input={destination,checkin:'2026-10-03',checkout:'2026-10-08',adults:4,rooms:2,bookerCountry:'ES'};
test('Booking provider sends v3.1 authentication and normalizes LIVE group prices', async()=>{let init:RequestInit|undefined;const provider=new BookingComAccommodationProvider('token','123','sandbox',async(_url,request)=>{init=request;return new Response(JSON.stringify({data:[{id:7,name:'Roma Hotel',currency:'EUR',price:{book:480},review_score:8.7,url:'https://booking.test/stay?aid=123'}]}),{status:200})});const result=await provider.search(input);assert.equal((init?.headers as Record<string,string>).Authorization,'Bearer token');assert.equal((init?.headers as Record<string,string>)['X-Affiliate-Id'],'123');assert.equal(result[0].totalStayPrice,480);assert.equal(result[0].status,'LIVE');});
test('Booking provider is unavailable without both credentials',()=>assert.equal(new BookingComAccommodationProvider().available,false));
test('Booking provider reports rate limits',async()=>{const provider=new BookingComAccommodationProvider('t','1','sandbox',async()=>new Response('',{status:429}));await assert.rejects(()=>provider.search(input),/limitado/)});
