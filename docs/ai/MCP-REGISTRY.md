# Registro MCP

Graphify se usa localmente mediante CLI y puede iniciarse en transporte stdio con
`graphify . --mcp` cuando el cliente MCP lo soporte. No hay servidor HTTP público ni
configuración MCP con credenciales en el repositorio. No se añaden otros servidores
hasta que resuelvan una necesidad concreta.
