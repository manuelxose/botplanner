# Optional: enables Graphify semantic extraction of docs/papers/images.
# Keep the real value in the process environment; never commit it here.
DEEPSEEK_API_KEY=
GRAPHIFY_DEEPSEEK_MODEL=deepseek-v4-pro[1m]
