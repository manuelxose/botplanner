# Airport dataset

`airports.json` is derived from [OurAirports](https://ourairports.com/data/), public-domain (Unlicense) data. Run `npm run data:airports:update` to fetch it; application startup never downloads it.
